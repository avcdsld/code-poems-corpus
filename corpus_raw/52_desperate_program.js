var ious= {
  holding: [ 'brie' + 'f' , { psuedo : 'secular'   },
    [ 'med', 'it', 'at', 'ions' ]
  ],
  message: "not understanding\n",
  barely: function( al ) { alert( 'when will you awake?' ); },

  // unrecoverable errors
  stop: function( ing ) {
    for ( var y = 0; y < ious[ 'holding' ][ 2 ].length; y++ ) {
      var ied = confirm( ious.message + ious[ 'holding' ][ 2 ][ y ] );
      if ( ! ied ) { setTimeout ( ious.barely, 10000 ); }
    }
  }
};
