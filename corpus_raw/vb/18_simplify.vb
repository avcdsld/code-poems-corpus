Module Simplicity
    Dim Simplicity
    Dim good
    Sub Main
        Simplicity = good
        ' To gain it,
        ' Distractions must be removed
        ' And clutter eradicated. So,
        ' Simplify! Remove the garbage!
        If My.Computer.FileSystem.FileExists("C:
\globdata.ini") = True Then
My. Computer.FileSystem.DeleteFile("C: \globdata.ini")
        Console.WriteLine("Now that you've simplified, relax.")
    End Sub
End Module
